# Starlight EasyEffects Presets

This directory is reserved for validated Starlight OS EasyEffects presets.

No preset is installed or enabled by default. Preset JSON files must be added
only after testing on real hardware profiles and must remain opt-in for users.
